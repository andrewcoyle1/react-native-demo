{
  "name": "stamina-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --watch src/index.ts",
    "start": "node --env-file-if-exists=.env --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/index.ts",
    "migrate": "node --env-file-if-exists=.env --disable-warning=MODULE_TYPELESS_PACKAGE_JSON src/migrate.ts",
    "test": "LOG_LEVEL=silent node --env-file-if-exists=.env --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test --test-concurrency=1 test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "argon2": "^0.45.1",
    "fastify": "^5.2.0",
    "jose": "^6.2.12",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "~6.0.3"
  }
}
